"""AlignNet is now integrated into VisualEncoder (encoders.py).
Ref: MATH.md M.1.2 Step 3 — AlignNet = Linear(512, d_text) + LayerNorm.

This file is kept for backwards compatibility of imports.
The actual AlignNet layers live inside VisualEncoder.align_net.
"""
