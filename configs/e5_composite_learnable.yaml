# E5: Full composite loss with LossMixer (learned weights)
# Ref: MATH.md M.3.5
experiment: e5_composite_learnable

loss:
  type: composite
  tau: 0.07
  lambda_align: 0.3
  lambda_rad: 0.1
  lambda_reg: 0.05
  lambda_va: 0.1

mixer:
  hidden_dim: 64
